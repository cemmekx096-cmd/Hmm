╔══════════════════════════════════════════════════════════╗
║          APK MODDING QUICK REFERENCE GUIDE               ║
╚══════════════════════════════════════════════════════════╝

📁 WORKSPACE LOCATION:
   ~/apk-modding/

🛠️ BASIC COMMANDS:

1. Decompile APK:
   apktool d app.apk -o app_decompiled

2. Compile APK:
   apktool b app_decompiled -o app_modded.apk

3. Decompile to Java (readable):
   jadx app.apk -d output_folder

4. Compare two folders:
   diff -qr folder1/ folder2/

5. Search in files:
   grep -r "search_term" folder/

6. Analyze .so file:
   strings file.so | grep "keyword"
   nm file.so | grep "function"
   r2 file.so

🎯 CUSTOM ALIASES:

- apkd <file>         : Decompile APK
- apkb <folder>       : Build APK
- apkgrep <dir> <term>: Search in APK
- apkcompare <d1> <d2>: Compare folders
- workspace           : Go to workspace
- mtdir              : Go to MT Manager folder

📝 TEXT EDITORS:

- nano file.smali    : Simple editor
- micro file.smali   : User-friendly editor
- vim file.smali     : Advanced editor

🔍 ANALYSIS TOOLS:

- file <file>        : Check file type
- xxd <file>         : Hex dump
- strings <file>     : Extract strings
- r2 <file>          : Radare2 analysis

📚 USEFUL LOCATIONS:

- APK structure after decompile:
  ├── smali/          (Java code)
  ├── lib/            (Native libraries .so)
  ├── res/            (Resources)
  ├── assets/         (Assets)
  └── AndroidManifest.xml

- MT Manager typical location:
  /sdcard/MT2/apks/

Need help? Type: man <command_name>
